import DonutMenu from "../components/DonutMenu";

const Home = () => (
  <div>

    <h2>Welcome to the Main Page</h2>
    <p>This page doesn’t use the shared layout.</p>
    <DonutMenu />
  </div>
);

export default Home;